---
title: Architecture Health
---

# Architecture Health

How well the Architecture Spec is currently backed: which requirements are
implemented, partial or planned, whether each names verifying evidence, and
which limitations are still open. Every figure on this page is computed from the
requirement tables and the `LIMIT-*` records when the site is built; nothing
here is written by hand, so it cannot drift from them.

Every limitation and gap, with its status, is listed on [Limitations and Gaps](limitations-and-gaps.md).

<!-- health:architecture -->
